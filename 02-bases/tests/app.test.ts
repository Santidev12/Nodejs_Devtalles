// A A A

describe('App', () => {

    test('should be 30', () => {

        // 1. Arrange

        const num1 = 10;
        const num2 = 20;

        // 1. Act
        const result = num1 + num2

        // 1. Assert
        expect(result).toBe(30)

    });
});